<Page
    x:Class="Tuuto.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tuuto.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:model="using:Mastodon.Model"
    xmlns:controls="using:Tuuto.Common.Controls"
    xmlns:behaviors="using:Tuuto.Common.Behaviors"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:Interactions="using:Microsoft.Xaml.Interactions.Core"
    xmlns:fa="using:FontAwesome.UWP"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="320" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="BottomCommandBar.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="720" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="BottomCommandBar.Visibility" Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <controls:FixedPivot x:Name="fixedPivot" x:Uid="MainPage_FixedPivot">
            <controls:FixedPivot.SplitViewPaneButtomContent>
                <StackPanel Orientation="Vertical">
                    <controls:SplitViewButton Click="{x:Bind SearchClick}" Icon="Search" x:Uid="MainPage_Search" />
                    <controls:SplitViewButton Click="{x:Bind RefreshClick}" Icon="Refresh" x:Uid="MainPage_Refresh"/>
                    <controls:SplitViewButton Click="{x:Bind ShowTootDialog}" Icon="Pencil" x:Uid="MainPage_Toot" x:Name="TootSplitViewButton">
                        <!-- TODO: Behaviors doesn't work -->
                        <!--<Interactivity:Interaction.Behaviors>
                            <Interactions:EventTriggerBehavior EventName="Tapped" SourceObject="{Binding ElementName=TootSplitViewButton}">
                                <behaviors:ShowTootDialog/>
                            </Interactions:EventTriggerBehavior>
                        </Interactivity:Interaction.Behaviors>-->
                    </controls:SplitViewButton>
                    <controls:SplitViewButton Click="{x:Bind SettingClick}" Icon="Cog" x:Uid="MainPage_Setting"/>
                </StackPanel>
            </controls:FixedPivot.SplitViewPaneButtomContent>
            <controls:FixedPivotItem x:Name="pivotitem" RefreshRequested="{x:Bind ViewModel.TimelineHome.Refresh}" HeaderIcon="Home" x:Uid="MainPage_Home">
                <controls:ExListView IsError="{x:Bind ViewModel.TimelineHome.IsError,Mode=OneWay}" RefreshRequested="{x:Bind ViewModel.TimelineHome.Refresh}" ItemsSource="{x:Bind ViewModel.TimelineHome}" IsLoading="{x:Bind ViewModel.TimelineHome.IsLoading,Mode=OneWay}">
                    <controls:ExListView.ItemTemplate>
                        <DataTemplate x:DataType="model:StatusModel">
                            <local:StatusView ViewModel="{x:Bind}"/>
                        </DataTemplate>
                    </controls:ExListView.ItemTemplate>
                </controls:ExListView>
            </controls:FixedPivotItem>
            <controls:FixedPivotItem RefreshRequested="{x:Bind ViewModel.Notification.Refresh}" HeaderIcon="Bell" x:Uid="MainPage_Notifications">
                <controls:ExListView IsError="{x:Bind ViewModel.Notification.IsError,Mode=OneWay}" RefreshRequested="{x:Bind ViewModel.Notification.Refresh}" ItemsSource="{x:Bind ViewModel.Notification}" IsLoading="{x:Bind ViewModel.Notification.IsLoading,Mode=OneWay}">
                    <controls:ExListView.ItemTemplate>
                        <DataTemplate x:DataType="model:NotificationModel">
                            <local:NotificationView ViewModel="{x:Bind}"/>
                        </DataTemplate>
                    </controls:ExListView.ItemTemplate>
                </controls:ExListView>
            </controls:FixedPivotItem>
            <controls:FixedPivotItem Visibility="{Binding Source={StaticResource Settings},Path=EnableLocalTimeline,Converter={StaticResource BoolToVisibilityConverter}}" RefreshRequested="{x:Bind ViewModel.TimelineLocal.Refresh}" HeaderIcon="Users" x:Uid="MainPage_Local">
                <controls:ExListView IsError="{x:Bind ViewModel.TimelineLocal.IsError,Mode=OneWay}" RefreshRequested="{x:Bind ViewModel.TimelineLocal.Refresh}" ItemsSource="{x:Bind ViewModel.TimelineLocal}" IsLoading="{x:Bind ViewModel.TimelineLocal.IsLoading,Mode=OneWay}">
                    <controls:ExListView.ItemTemplate>
                        <DataTemplate x:DataType="model:StatusModel">
                            <local:StatusView ViewModel="{x:Bind}"/>
                        </DataTemplate>
                    </controls:ExListView.ItemTemplate>
                </controls:ExListView>
            </controls:FixedPivotItem>
            <controls:FixedPivotItem Visibility="{Binding Source={StaticResource Settings},Path=EnableFederatedTimeline,Converter={StaticResource BoolToVisibilityConverter}}" RefreshRequested="{x:Bind ViewModel.TimelineFederated.Refresh}" HeaderIcon="Globe" x:Uid="MainPage_Federated">
                <controls:ExListView IsError="{x:Bind ViewModel.TimelineFederated.IsError,Mode=OneWay}" RefreshRequested="{x:Bind ViewModel.TimelineFederated.Refresh}" ItemsSource="{x:Bind ViewModel.TimelineFederated}" IsLoading="{x:Bind ViewModel.TimelineFederated.IsLoading,Mode=OneWay}">
                    <controls:ExListView.ItemTemplate>
                        <DataTemplate x:DataType="model:StatusModel">
                            <local:StatusView ViewModel="{x:Bind}"/>
                        </DataTemplate>
                    </controls:ExListView.ItemTemplate>
                </controls:ExListView>
            </controls:FixedPivotItem>
            <controls:FixedPivotItem RefreshRequested="{x:Bind ViewModel.Account.Refresh}" HeaderIcon="User" x:Uid="MainPage_Account">
                <local:AccountView IsError="{x:Bind ViewModel.Account.AccountTimeline.IsError,Mode=OneWay}" RefreshRequested="{x:Bind ViewModel.Account.Refresh}" IsLoading="{x:Bind ViewModel.Account.AccountTimeline.IsLoading,Mode=OneWay}" ItemsSource="{x:Bind ViewModel.Account.AccountTimeline}" Account="{x:Bind ViewModel.Account.Account.Result,Mode=OneWay}"/>
            </controls:FixedPivotItem>
        </controls:FixedPivot>
    </Grid>
    <Page.BottomAppBar>
        <CommandBar  x:Name="BottomCommandBar">
            <AppBarButton x:Uid="MainPage_Search">
                <AppBarButton.Icon>
                    <fa:FontAwesome Icon="Search"/>
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton Click="{x:Bind RefreshClick}" x:Uid="MainPage_Refresh">
                <AppBarButton.Icon>
                    <fa:FontAwesome Icon="Refresh"/>
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton x:Uid="MainPage_Toot" x:Name="TootAppBarButton">
                <Interactivity:Interaction.Behaviors>
                    <Interactions:EventTriggerBehavior EventName="Click" SourceObject="{Binding ElementName=TootAppBarButton}">
                        <behaviors:ShowTootDialog/>
                    </Interactions:EventTriggerBehavior>
                </Interactivity:Interaction.Behaviors>
                <AppBarButton.Icon>
                    <fa:FontAwesome Icon="Pencil"/>
                </AppBarButton.Icon>
            </AppBarButton>
            <CommandBar.SecondaryCommands>
                <AppBarButton Click="{x:Bind SettingClick}" Icon="Setting" x:Uid="MainPage_Setting"/>
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Page.BottomAppBar>
</Page>
